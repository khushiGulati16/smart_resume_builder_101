<!DOCTYPE html>
<html>
<head>
  <title><%= resume.name %> - Resume</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="template template2">
  <div class="resume-alt main-resume-container">
    <header>
      <h1><%= resume.name %></h1>
      <p class="role-title"><%= resume.role %></p>
      <div class="contact-info">
        <span><%= resume.email %></span> |
        <span><%= resume.phone %></span> |
        <span><%= resume.address %></span> |
        <span><%= resume.linkedin %></span>
      </div>
    </header>

    <main>
      <div class="block">
        <h3>Education</h3>
        <p><%= resume.education %></p>
      </div>

      <div class="block">
        <h3>Work Experience</h3>
        <% if (resume.experience1) { %>
          <p><%= resume.experience1 %></p>
        <% } %>
        <% if (resume.experience2) { %>
          <p><%= resume.experience2 %></p>
        <% } %>
        <% if (resume.experience3) { %>
          <p><%= resume.experience3 %></p>
        <% } %>
      </div>

      <div class="block">
        <h3>Skills</h3>
        <p><%= resume.skills %></p>
      </div>
    </main>
  </div>
  <a href="/api/resume/download/<%= id %>" class="download-button">Download as PDF</a>
</body>
</html>